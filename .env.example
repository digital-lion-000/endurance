# Optional but recommended to avoid GitHub rate limits.
# Create a fine-grained or classic PAT with public repo read access.
GITHUB_TOKEN=

# Optional default search value
NEXT_PUBLIC_DEFAULT_QUERY=react
